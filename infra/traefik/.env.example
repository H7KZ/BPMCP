# Traefik .env

# Email Let's Encrypt uses for the ACME account (expiry notices)
ACME_EMAIL=you@example.com

# Only needed for the Cloudflare DNS-01 alternative (domain behind CF proxy)
# Uncomment here AND the matching lines in docker-compose.traefik.yml
# CF_DNS_API_TOKEN=
