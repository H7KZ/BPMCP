# Grafana Alerting Notification Policies

apiVersion: 1

policies:
  - orgId: 1
    receiver: discord
    group_by: [ alertname, env, severity ]
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 6h
